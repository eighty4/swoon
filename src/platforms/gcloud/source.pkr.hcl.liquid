source "googlecompute" "{{ source_label }}" {
    project_id   = "{{ project_id }}"
    image_name   = "{{ image_name }}"
    {{ source_image_method }} = "{{ source_image_value }}"
    ssh_username = "packer"
    zone         = "us-central1-a"
    machine_type = "e2-medium"
}
